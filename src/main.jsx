import React from "react";
import ReactDOM from "react-dom/client";
import BuyersTable from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <BuyersTable />
  </React.StrictMode>
);